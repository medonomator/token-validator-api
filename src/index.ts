const PORT = Number(process.env.PORT ?? 3000);

function main(): void {
  console.log(`token-validator-api ready on port ${PORT}`);
  // TODO: see README Tasks 1-3 for healthcheck, core endpoint, and validation.
}

main();